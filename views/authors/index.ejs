<%- include('../partials/head') %> <%- include('../partials/header') %>
<main>
  <h2>Find Your Favorite Author</h2>

  <table>
    <thead>
      <tr>        
        <th>Author</th>
        <th>Title</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% authors.forEach(a => { %>
      <tr>
        <td><%= a.firstName %> <%= a.middleName %> <%= a.lastName %></td>
        <td>
          <% a.booksWritten.forEach(function(b, i) { %>
          <p><%= b.title %></p>
          <% }) %>
        </td>
        <td><a href="/authors/<%= a._id %>/">Details</a></td>
      </tr>
      <% }) %>
    </tbody>
  </table>
</main>

<%- include('../partials/footer') %>
